# auto

`kubash auto`

or with a named cluster

`kubash -n mycluster auto`

This will parse your hosts.csv and then run all the initializer recipes other than build. kubash {init,openebs,tiller,ingress}
